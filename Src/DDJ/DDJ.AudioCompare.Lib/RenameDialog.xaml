<Window x:Class="DDJ.AudioCompare.Lib.RenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RenameDialog" Height="208" Width="727">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock	Margin="6" Text="{Binding FileName}" Grid.Row="0" />
		<TextBox		Margin="6" Text="{Binding FileName}" Grid.Row="1" />
		<StackPanel Margin="6" Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
			<Button Content="OK" IsDefault="True"			Margin="6" Width="60" Click="Button_Click" />
			<Button Content="Cancel" IsCancel="True"	Margin="6" Width="60" Click="Button_Click_1" />

		</StackPanel>

	</Grid>
</Window>
